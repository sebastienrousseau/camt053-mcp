# Runtime + test dependencies for the CI "test" job.
# Compiled to test.txt with hashes via: make pip-compile
mcp>=1.2
camt053>=0.0.6,<1
camt053-loader-mt940>=0.0.9
camt053-loader-mt942>=0.0.1
pytest>=9.0.3,<10.0.0
pytest-asyncio>=0.24,<2.0
pytest-cov>=6.0.0,<8.0.0
interrogate>=1.7
pyjwt[crypto]>=2.10,<3
httpx>=0.27,<1
prometheus-client>=0.20,<2
# Optional [otel] tracing extra: required in the test environment so the
# tracing suite (tests/test_tracing.py) can import OpenTelemetry and verify
# real spans against an InMemorySpanExporter. Not part of the base install.
opentelemetry-sdk>=1.20,<2
opentelemetry-exporter-otlp-proto-http>=1.20,<2
# Optional [vector] search extra: required in the test environment so the
# vector-search suite (tests/test_vector_search.py) can build a real
# sqlite-vec index over the curated rulebook clauses. Not part of the base
# install.
sqlite-vec>=0.1.6,<0.2
